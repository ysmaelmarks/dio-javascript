// função autoinvocável

(
function(){
código
}
) ();
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

// callbacks

const teste = function(x, y, z){
	return x (y, z)
}

const soma = function(y, z){
	return x + y;
}

const subtracao = function(x, y){
	return x - y;
}

const resultadoSoma = teste(soma, y, z);
const resultadoSubtracao = teste(subtracao, y, z);
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
